apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: nri-plugin-template
spec:
  template:
    spec:
      containers:
        - name: plugin
          image: "ghcr.io/containerd/nri/plugins/template:unstable"
          imagePullPolicy: Always
          args:
            - "-idx"
            - "99"
            - "-name"
            - "template"
          volumeMounts:
            - name: nri-socket
              mountPath: /var/run/nri/nri.sock
      volumes:
      - name: nri-socket
        hostPath:
          path: /var/run/nri/nri.sock
          type: Socket
